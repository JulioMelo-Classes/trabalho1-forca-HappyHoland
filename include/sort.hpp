#include <iostream>
#include <vector>

/**
 * @brief recebe um vetor de strings e o ordena de forma aleatória
 * @param palavras - um vetor de strings
 */
void sort (std::vector<std::string>& palavras);